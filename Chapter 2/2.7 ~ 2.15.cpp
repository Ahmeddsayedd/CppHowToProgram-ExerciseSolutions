// 2.7

// a) std::cin is used to receive input from the keyboard. It's essentially the standard input stream that reads data entered by the user.
// b) std::cout is used to display output to the screen, acting as the standard output stream that prints messages, results, or any information that you want the user to see.


// >>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

// 2.8

// a) Comments
// b) std::cout
// c) if
// d) assignment
// e) std::cin


// >>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

// 2.9

// a) std::cout << "Enter two numbers" << std::endl;
// b) a = b * c;
// c) // This program performs a payroll calculation
// d) std::cin >> a >> b >> c;


// >>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

// 2.10

// a) False. C++ operators evaluation depends on their precedence and associativity. Some operators are evaluated from left to right, while others (like the assignment operator =) are evaluated from right to left.
// b) True.
// c) False. The statement cout << "a = 5;"; is not an assignment statement; it's an output statement that prints "a = 5;".
// d) False. The evaluation order depends on operator precedence and associativity, not just the left-to-right sequence.
// e) False, h22 is a valid variable name. All of the other variable names start with digits and are therefore invalid variable names.


// >>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

// 2.11

// a) Division and modulus
// b) The innermost parentheses
// c) variable


// >>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

// 2.12

// a) 2
// b) 4
// c) x=
// d) x= 2
// e) 5 = 5
// f) nothing
// g) nothing
// h) nothing
// i) nothing "New line"


// >>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

// 2.13

// The answer is the choices
// a) cin >> b >> c >> d >> e >> f; takes input from the user and assigns (or replaces) the values of the variables b, c, d, e, and f.
// and also b)  p = i + j + k + 7; calculates the sum of i, j, k, and 7, and then assigns (or replaces) the value of the variable p with the result.


// >>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

// 2.14

// The correct statements are:
// a) y = a * x * x * x + 7
// d) y = (a * x) * x * x + 7;
// e) y = a * ( x * x * x ) + 7;


// >>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

// 2.15

// a)
//     x = 7 + 3 * 6 / 2- 1;
//     x = 7 + 18 / 2 -1;
//     x = 7 + 9 - 1;
//     x = 16 - 1 = 15

// b)
//     x = 2 % 2 + 2 * 2 - 2 / 2;
//     x = 0 + 2 * 2 - 2 / 2;
//     x = 0 + 4 - 2 / 2;
//     x = 0 + 4 - 1;
//     x = 4 - 1 = 3

// c)
//    x = ( 3 * 9 * ( 3 + ( 9 * 3 / ( 3 ) ) ) );
//    x = ( 3 * 9 * ( 3 + ( 9 * 3 / 3 ) ) );
//    x = ( 3 * 9 * ( 3 + ( 27 / 3 ) ) );
//    x = ( 3 * 9 * ( 3 + 9 ) );
//    x = ( 3 * 9 * 12 );
//    x = ( 27 * 12 );
//    x = 324



